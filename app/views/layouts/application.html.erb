<!DOCTYPE html>
<html>
  <head>
    <title>Ride</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <main class="flex flex-col h-full">
    <%= render template: 'components/header' %>
    <div class="flex h-screen fixed top-20 w-full">
      <div class="w-4/12 md-w-2/12 bg-gray-50 border-r border-gray-200 ">
        <%= render template: 'components/filter_form' %>
      </div>
      <div class="w-10/12 h-full bg-slate-50 overflow-auto">
        <%= yield %>
      </div>
    </div>
  </main>
  </body>
</html>
