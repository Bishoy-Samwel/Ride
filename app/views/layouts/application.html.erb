<!DOCTYPE html>
<html>
  <head>
    <title>Ride</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <main class="flex flex-col h-full">
    <%= render template: 'components/header' %>
    <div class="flex h-screen">
      <div class="w-2/12 bg-gray-50">
        <%= render template: 'components/filter_form' %>
      </div>
      <div class="w-10/12">
        <%= yield %>
      </div>
    </div>
  </main>
  </body>
</html>
