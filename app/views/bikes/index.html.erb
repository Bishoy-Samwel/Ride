  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div id="bikes" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 w-full  bg-slate-50 p-10">
  <%= render @bikes %>
  </div>
